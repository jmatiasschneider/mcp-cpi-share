# Una dependencia nueva se guarda con version exacta (1.2.3), no con rango (^1.2.3).
# El arbol instalado lo fija package-lock.json; instalar con `npm ci`, no con `npm install`.
save-exact=true

# `engines.node` de package.json bloquea en vez de solo avisar.
engine-strict=true
